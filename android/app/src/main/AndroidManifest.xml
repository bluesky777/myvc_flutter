<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- El SDK de Analytics declara SOLO el permiso del identificador de
         publicidad, aunque esta app no tenga un anuncio. Aquí no se quiere: no
         hay publicidad, no hay perfiles y es una app de menores. Hacen falta
         las dos piezas, no una: este tools:node lo saca del manifiesto
         fusionado, y el meta-data de abajo apaga la recogida. Sin la primera,
         el permiso llega al bundle y Play lo señala en seguridad de datos; y
         publicacion-play.md §9 dice «solo INTERNET, no hay que justificar
         nada», que es algo que conviene que siga siendo verdad.
         Ver docs/analitica.md → «Lo que se apaga a propósito». -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID"
        tools:node="remove" />

    <!-- Y las dos del Privacy Sandbox que trae el mismo SDK: el identificador
         de publicidad nuevo y la atribución de anuncios. Se van por lo mismo
         que la de arriba —no hay anuncios que atribuir—, y comprobado en el
         manifiesto fusionado, no supuesto.

         NO se quitan ACCESS_NETWORK_STATE ni WAKE_LOCK, que también entran con
         Firebase: esas son funcionales —mirar si hay red antes de subir un lote
         de eventos, y no dormirse a mitad—, y quitarlas rompería la subida en
         vez de proteger a nadie. Ninguna de las dos pide nada al usuario. -->
    <uses-permission android:name="android.permission.ACCESS_ADSERVICES_AD_ID"
        tools:node="remove" />
    <uses-permission android:name="android.permission.ACCESS_ADSERVICES_ATTRIBUTION"
        tools:node="remove" />

    <application
        android:label="Mi Cole Virtual"
        android:name="${applicationName}"
        android:icon="@mipmap/launcher_icon">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- La otra mitad de lo de arriba: apaga la recogida del identificador
             de publicidad en el propio SDK. -->
        <meta-data
            android:name="google_analytics_adid_collection_enabled"
            android:value="false" />
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
