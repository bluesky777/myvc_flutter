import 'package:flutter/material.dart';

const kPrimaryColor = Color(0XFF6A62B7);
const KBackgroundColor = Color(0XFFE5E5E5);

/// Los colores con los que se señala una falta a la institución en la lista.
///
/// Son los dos de la misma familia que el color de la app, para que se lean
/// como dos grados de lo mismo y no como dos avisos sin relación: rosa el que
/// llegó tarde, morado el que no vino.
const kColorTardanza = Color(0XFFFF7FA8);
const kColorAusencia = Color(0XFFB47CE0);
